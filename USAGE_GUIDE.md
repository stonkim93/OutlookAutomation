# 📖 사용 가이드

## 🎯 기본 사용 흐름

```
시작
  ↓
[1단계: 파일 선택]
  ↓
[2단계: 폴더 선택]
  ↓
[자동화 실행]
  ↓
진행 상황 표시
  ├─ ✓ 파일 열기
  ├─ ✓ Outlook 시작
  ├─ ✓ 메일 작성
  ├─ ✓ 파일 첨부
  └─ ✓ 저장 및 압축
  ↓
완료
```

## 📖 단계별 가이드

### 1단계: 앱 실행

```
1. OutlookAutomationApp.exe 실행
2. 앱 창이 나타남
```

### 2단계: 파일 선택

```
1. "파일 선택..." 버튼 클릭
2. 파일 선택 대화상자 열림
3. 첨부할 파일 선택
   ✓ Excel: .xlsx, .xls
   ✓ Word: .docx, .doc
   ✓ PowerPoint: .pptx, .ppt
   ✓ PDF: .pdf
   ✓ 한글: .hwp, .hwpx
   ✓ 이미지: .png, .jpg, .bmp
   ✓ 텍스트: .txt
   ✓ 기타: 모든 파일

4. "열기" 버튼 클릭
5. 상태: "📄 파일 선택됨: filename"
```

**팁**: 최근 파일은 다시 선택할 수 있습니다.

### 3단계: 폴더 선택

```
1. "폴더 선택..." 버튼 클릭
2. 폴더 선택 대화상자 열림
3. 파일을 저장할 폴더 선택
   예: C:\Output, C:\Users\Documents 등

4. "확인" 버튼 클릭
5. 상태: "📁 폴더 선택됨: path"
```

**팁**: 새 폴더 버튼으로 새 폴더를 바로 만들 수 있습니다.

### 4단계: 자동화 실행

```
1. "자동화 실행" 버튼 클릭
2. 실행 중 UI 비활성화 (실수 방지)
3. 실시간 진행 상황 표시:

   [1/5] 파일을 앱으로 열고 있습니다...
   ✅ Excel로 파일 열기 완료

   [2/5] Outlook 초기화 중...
   ✅ Outlook 초기화 완료

   [3/5] 메일 작성 중...
   ✅ 임시 메일 작성 완료

   [4/5] 파일 첨부 중...
   ✅ 파일 첨부 완료

   [5/5] 저장 및 압축 중...
   ✅ 파일 저장 완료
   ✅ ZIP 압축 완료: output.zip

4. 완료 메시지 표시
```

### 5단계: 결과 확인

자동화 실행 후 **출력 폴더**에서:

```
C:\Output\
├── report.xlsx              (원본 파일 복사)
└── report_20240115.zip      (자동 생성된 압축 파일)
```

## 📌 주요 기능

### 📄 파일 자동 열기

선택한 파일을 **Windows 기본 프로그램**으로 자동 엽니다.

```
파일 확장자 → 자동 실행
━━━━━━━━━━━━━━━━━━━━━
.xlsx       → Excel
.docx       → Word
.pptx       → PowerPoint
.pdf        → PDF Reader
.hwp        → 한글(한컴 오피스)
.jpg        → 이미지 뷰어
.txt        → 메모장
```

**참고**: 기본 프로그램이 설정되어 있어야 합니다.

### 📧 Outlook 자동 연동

두 가지 Outlook 환경 모두 지원합니다.

#### Classic Outlook (Office 365, 2019 등)

```
장점:
✓ COM Interop로 완전 자동화
✓ 백그라운드 연동 가능
✓ 높은 안정성

방식: Windows COM 객체 활용
```

#### New Outlook (웹 기반)

```
특징:
✓ COM 미지원 (최신 웹 기반)
✓ MAPI 연동으로 호환
✓ 파일 복사 방식 사용

방식: MAPI32.DLL 또는 파일 복사
```

**자동 감지**: 설치된 Outlook 버전 자동 인식

### 💾 파일 자동 저장

```
원본: report.xlsx
저장: report.xlsx
```

지정한 출력 폴더에 저장됩니다.

### 📦 ZIP 압축 자동 생성

```
저장된 파일: report.xlsx
압축 파일: report_20240115_143025.zip

압축률: 최적화 (기본)
크기: 원본의 30~50%
```

### 🛡️ 파일명 충돌 방지

동일한 이름의 파일이 있으면 **타임스탬프 자동 추가**:

```
1번째 저장: report.xlsx
2번째 저장: report_20240115_143025.xlsx
3번째 저장: report_20240115_143026.xlsx

형식: filename_YYYYMMdd_HHmmss.ext
```

### 🔄 실시간 상태 표시

```
상태 표시줄:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━
대기 중...
파일을 선택하세요

선택 후:
📄 파일 선택됨: report.xlsx

폴더 선택 후:
✅ 모든 항목 준비 완료
[자동화 실행] 버튼 활성화

실행 중:
✅ [1/5] 파일 열기 완료
✅ [2/5] Outlook 초기화 완료
...

완료 시:
✅ 자동화 완료!
```

## 💡 팁 & 트릭

### Tip 1: 빠른 재실행

같은 파일/폴더로 다시 실행하려면:

```
1. 파일과 폴더를 이미 선택한 상태
2. "자동화 실행" 버튼만 클릭
3. 새 파일명이 자동 생성됨
```

### Tip 2: 배치 처리

같은 폴더의 여러 파일을 처리:

```
파일 1: 선택 → 폴더 선택 → 실행
파일 2: 선택 → (폴더는 이미 선택됨) → 실행
파일 3: 선택 → 실행
```

### Tip 3: 저장 폴더 열기

저장 완료 후 폴더를 자동으로 열기:

```bash
# Windows 탐색기에서 저장 폴더 위치 확인
# 또는 "Windows + E" 후 경로 입력
```

### Tip 4: 작업 스케줄러에 등록

Windows 작업 스케줄러로 **정기적 실행**:

```
1. Win + R → taskschd.msc
2. "기본 작업 만들기"
3. 프로그램: OutlookAutomationApp.exe
4. 일정: 매일 오전 9시
```

## 🆘 문제 해결

### "파일을 선택하세요" 메시지

**문제**: 파일이 선택되지 않음

**해결책**:
```
1. "파일 선택..." 버튼 다시 클릭
2. 파일 경로 확인 (공백, 특수문자 없는지)
3. 파일이 존재하는지 확인
4. 파일이 현재 열려있지 않은지 확인
```

### "폴더를 선택하세요" 메시지

**문제**: 폴더가 선택되지 않음

**해결책**:
```
1. "폴더 선택..." 버튼 다시 클릭
2. 폴더 경로 확인
3. 쓰기 권한이 있는지 확인
4. 폴더가 존재하는지 확인
```

### "[1/5] 파일 열기 실패"

**문제**: 파일이 열리지 않음

**원인**:
- 파일의 기본 프로그램이 설정되지 않음
- 프로그램이 설치되지 않음

**해결책**:
```
1. Windows 탐색기에서 파일 우클릭
2. "연결 방법" → 기본 프로그램 설정
3. 앱 다시 실행
```

### "[2/5] Outlook 초기화 실패"

**문제**: Outlook을 찾을 수 없음

**원인**:
- Outlook이 설치되지 않음
- Outlook 버전 불일치

**해결책**:
```
1. Microsoft Outlook 설치 확인
2. Outlook 실행
3. 앱 다시 실행
```

### "[4/5] 파일 첨부 실패"

**문제**: 파일이 첨부되지 않음

**원인**:
- 파일이 다른 앱에서 열려 있음
- 파일 경로가 너무 길거나 특수문자 포함

**해결책**:
```
1. 파일을 열고 있는 앱 모두 종료
2. 다시 시도
```

### "[5/5] 저장 실패"

**문제**: 파일이 저장되지 않음

**원인**:
- 출력 폴더에 쓰기 권한 없음
- 디스크 용량 부족
- 폴더가 네트워크 드라이브

**해결책**:
```
1. 로컬 폴더 선택 (C:\Output 등)
2. 폴더 권한 확인
3. 디스크 용량 확인 (최소 1GB)
```

### 자동화 실행 중 응답 없음

**문제**: 프로그램이 멈춘 것 같음

**원인**:
- 큰 파일 처리 중
- Outlook 응답 대기 중

**해결책**:
```
1. 30초 이상 기다리기
2. 작은 파일로 테스트
3. 작업 관리자에서 강제 종료 후 다시 시도
```

## 🔍 고급 기능

### 로그 확인

실행 중 콘솔 로그 확인:

```bash
# 앱 실행 시 콘솔 창 보기:
# 프로그램 실행 후 메시지 확인
```

### 타임스탬프 형식

자동 생성된 파일명:

```
형식: original_YYYYMMdd_HHmmss.ext
예시: report_20240115_143025.xlsx
      ↑      ↑
      |      └─ 시간: 14시 30분 25초
      └─────── 날짜: 2024년 1월 15일
```

### ZIP 압축 옵션

ZIP 파일에는 다음이 포함됩니다:

```
report.zip
└── report.xlsx (원본 파일)
```

압축률은 파일 종류에 따라 다릅니다:
- Excel: 30~50% 압축
- Word: 40~60% 압축
- PDF: 5~15% 압축 (이미 압축됨)

---

**도움이 더 필요하신가요?** [FAQ](FAQ.md)를 참고하거나 Issues를 등록하세요.
